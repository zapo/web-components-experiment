Experiment building a SPA in vanilla JS (no dependencies, no code transpilation/bundler)
exclusively using web components.

Run it using `npx serve --single` and open `http://localhost:5000` in some recent browser.
